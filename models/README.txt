hero.glb — Khronos glTF-Sample-Models "Duck" (120KB, valid GLB).
License: KhronosGroup/glTF-Sample-Models repo license (per-model LICENSE files);
swap with any CC0 rocket/books GLB (Poly Pizza / Quill by Icosa / Google Scanned
Objects, Draco <800KB) — hero-3d.ts lazy-loads it only when [data-glb] is visible,
missing/invalid file falls back silently to the three.js constellation.
